package org.projects.tradingservice.core;

public enum OrderStatus {
  OPEN, // Order is open
  FILLED, // Order is filled
  PART_FILLED, // Order is filled
  CANCELLED, // Order is cancelled
  REJECTED // Order is rejected
}
